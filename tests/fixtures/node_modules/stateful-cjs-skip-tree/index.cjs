const incrementPath = require.resolve('../../workspace-dynamic-increment-skip-tree.cjs');
require(incrementPath);

const exportedObject = {};

Object.defineProperty(exportedObject, 'run', {
	value() {
		const increment = require(incrementPath);
		return increment();
	},
	configurable: false,
	writable: false,
	enumerable: true,
});

module.exports = exportedObject;
