export function runDelayedImport() {
	return new Promise(resolve => {
		setTimeout(async () => {
			const {default: increment} = await import('../../workspace-dynamic-increment-delayed.js');
			resolve(increment());
		}, 0);
	});
}
