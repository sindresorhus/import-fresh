export default async function incrementWorkspaceModule() {
	await new Promise(resolve => {
		setTimeout(resolve, 0);
	});

	const {default: increment} = await import('../../workspace-dynamic-increment-concurrent.js');
	return increment();
}
